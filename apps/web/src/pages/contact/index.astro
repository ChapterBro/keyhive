---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { contact } from '../../config/contact';
import { createLocalBusinessSchema } from '../../utils/schema';
import { withTrailingSlash } from '../../utils/urls';

const canonicalPath = '/contact/';
---

<BaseLayout
  title="Contact KeyHive"
  description="Contact KeyHive for 24/7 mobile locksmith service in Kansas City, KS. Call or email for fast assistance."
  canonicalPath={canonicalPath}
  jsonLd={createLocalBusinessSchema(canonicalPath)}
>
  <section class="container py-14">
    <div class="grid gap-10 lg:grid-cols-[1.1fr_0.9fr]">
      <div class="space-y-6">
        <p class="text-xs font-semibold uppercase tracking-[0.35em] text-slate-500">Contact</p>
        <h1 class="font-display text-4xl text-slate-900">Reach KeyHive anytime</h1>
        <p class="text-lg text-slate-700">
          We are a 24/7 mobile locksmith serving Kansas City, KS and nearby communities. Call for immediate help or email with questions.
        </p>
        <div class="space-y-4 text-base text-slate-700">
          <div>
            <p class="text-sm font-semibold uppercase tracking-[0.3em] text-slate-500">Phone</p>
            <a class="text-xl font-semibold text-slate-900" href={contact.phoneHref}>{contact.phoneDisplay}</a>
          </div>
          <div>
            <p class="text-sm font-semibold uppercase tracking-[0.3em] text-slate-500">Email</p>
            <a class="text-lg font-semibold text-slate-900" href={`mailto:${contact.email}`}>{contact.email}</a>
          </div>
          <div>
            <p class="text-sm font-semibold uppercase tracking-[0.3em] text-slate-500">Hours</p>
            <p class="text-lg font-semibold text-slate-900">{contact.hours}</p>
          </div>
        </div>
        <div class="flex flex-col gap-3 sm:flex-row">
          <a
            class="inline-flex items-center justify-center rounded-full bg-accent px-6 py-3 text-base font-semibold text-slate-900"
            href={contact.phoneHref}
          >
            Call Now
          </a>
          <a
            class="inline-flex items-center justify-center rounded-full border border-slate-200 bg-white px-6 py-3 text-base font-semibold text-slate-700"
            href={withTrailingSlash('/services')}
          >
            View services
          </a>
        </div>
      </div>
      <div class="space-y-6 rounded-3xl border border-white/80 bg-white/90 p-6 shadow-soft">
        <h2 class="text-lg font-semibold text-slate-900">Service area focus</h2>
        <p class="text-sm text-slate-700">
          We service Kansas City, KS and the surrounding Kansas communities. If you are nearby, call to confirm availability.
        </p>
        <div class="rounded-2xl border border-slate-100 bg-white px-4 py-3 text-sm text-slate-700">
          Primary city: {contact.primaryCity}
        </div>
        <div class="rounded-2xl border border-slate-100 bg-white px-4 py-3 text-sm text-slate-700">
          Coverage includes residential, commercial, and automotive service calls.
        </div>
      </div>
    </div>
  </section>
</BaseLayout>
