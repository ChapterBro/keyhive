# KeyHive Web

Astro + Tailwind site for KeyHive.

## Commands

All commands run from the repo root:

```sh
pnpm -C apps/web dev
pnpm -C apps/web lint
pnpm -C apps/web build
pnpm -C apps/web preview
```
